{
  "name": "website",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "docusaurus start",
    "build": "docusaurus build",
    "swizzle": "docusaurus swizzle",
    "deploy": "npx cross-env GIT_USER=prma85 docusaurus deploy",
    "postinstall_old": "node -e \"try { require('fs').symlinkSync(require('path').resolve('./static/resources'), require('path').resolve('../') + '/resources') } catch (e) {}\""
  },
  "dependencies": {
    "@docusaurus/core": "^2.3.1",
    "@docusaurus/preset-classic": "^3.5.0",
    "react": "^16.14.0",
    "react-dom": "^16.14.0"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
